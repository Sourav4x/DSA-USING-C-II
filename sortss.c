#include<stdio.h>
#include<stdlib.h>
int selectionsort(int j[],int n)
{
    int i,j,k;
    for(i)
}
void main()
{
    int a,n;
printf("enter the no of elements in the array :-- ");
scanf("%d",&n);
a=(int*)malloc(n*sizeof(int));
selectionsort(a,n);
}